{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug PennyLedger",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/pennyledger",
            "output": "${workspaceFolder}/bin/__debug_bin.exe"
        }
    ]
}